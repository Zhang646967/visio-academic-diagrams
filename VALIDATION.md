# 验证记录 — v0.2.0

日期：2026-09-12。这里报告实际执行的检查，不将设计意图写成已完成的能力评测。

## 已完成

| 检查 | 结果 | 范围 |
|---|---|---|
| Python单元测试 | 74项通过 | 保留原版26项；新增论文来源/关系、投影、功能查询及发布输入/清单安全检查 |
| 论文→语义图→名义布局 | 4组通过，0错误/0警告 | 脑图、算法流程、相关关系、证明依赖 |
| JSON Schema元结构 | 4份通过 | paper-model1.0、figure-plan1.0、几何1.0及1.1 |
| 示例Schema匹配 | 12次匹配通过 | 4组三文件；两视图共享的paper-model重复验证 |
| 功能注册 | 220条、26功能族 | 54条document-backed；166条index-only；ID/来源/模块对应 |
| 来源注册 | 57项 | 43微软、9出版/图形研究、2研究规范/实践、3技能/CLI来源 |
| 本次来源阅读标记 | 新增19、重读10、继承28 | 继承记录没有伪装成本次逐篇重读 |
| 包结构与链接 | 101文件、225处相对链接检查通过 | 非空必需文件、JSON、主入口、相对文件链接与来源ID；不校验Markdown内部锚点 |

真实测试输出：[tests/last-run.txt](tests/last-run.txt)。配套结果：[tests/bundle-results.json](tests/bundle-results.json)。结构检查可用`python scripts/validate_package.py`重现；最终文件清单见`manifest.sha256`。

JSON Schema交叉检查额外使用制作环境现成的jsonschema库；本包交付的检查脚本不依赖该库。`validate_research.py`中的内置检查器仅实现本包两份研究Schema使用的子集，不宣称支持完整JSON Schema标准。

一次批量交叉检查因工具调用超时未返回完整结果；已用显式逐文件检查重新完成上述4份Schema和12次实例验证。最终表格基于完成的重跑结果。

## 发布预检

`publish_github.py`默认演练已通过，检查101个文件，未联网。随后在用户授权范围内尝试显式发布，但在gh可用性检查时失败：`gh is not installed on this machine`。此前未发生远程写入。

这说明本地发布预检可运行，不代表线上建仓、推送和远程校验已通过。

## 没有完成，因此不能宣称通过

- 未在用户真实Visio桌面/网页环境逐步试做；所有GUI/运行测试标记仍是false。
- 未检验真实字体排版、连接点粘附、容器成员、隐藏Shape Data、VSDX再次打开或导出字体嵌入。
- 未使用真实论文做独立学科专家评审或衡量任意论文理解准确率；示例均为原创合成材料。
- 未运行独立模型的技能触发率/任务成功率实验。[行为场景](tests/scenarios.md)仅是未来可执行的评测设计。
- 未完整穷举所有Visio命令、模具、模板、版别或API成员；index-only不能视为精确操作已核验。
- 未逐个实时检查所有外链和全部期刊当期指南；只有标明本次重读/新增的范围做了相应来源核验。
- 未完成GitHub远程建仓与推送；公开发布脚本的线上路径未经实测。见[发布状态](docs/PUBLICATION-STATUS.md)。

## 检查的解释边界

结构化来源关联可以发现缺少引用、无依据提升确定性、显式关系类型混用等问题，但不能判断引用是否真正支持自然语言陈述。图种选择和科学论证仍须回到材料本身。

名义坐标检查可以发现部分尺寸、重叠、回边及缩放问题；它不是Visio渲染器。线标签与真实字宽的冲突、公式显示、复杂嵌套对象等仍需实际成图检查。

74项单元测试通过率不是“74篇论文理解正确”，也不是“期刊接受率100%”。来源中的procedure-reviewed不是GUI-tested。SHA-256用于文件完整性，不是签名或隐私审查替代品。

原版记录保留于[历史验证](docs/VALIDATION-v0.1.0.md)，不用于描述当前版本。
