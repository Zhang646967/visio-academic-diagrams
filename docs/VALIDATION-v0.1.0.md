# 实际执行的验证记录

日期：2026-09-12；版本0.1.0。验证环境：Python 3.13.5，Linux容器。

## 已执行并通过

| 检查 | 本次结果 | 说明 |
|---|---|---|
| 技能YAML | 通过 | 主文件及agents/openai.yaml可解析；名称/描述/metadata类型检查通过 |
| SKILL.md长度 | 78行 | 采用短入口+按需读取模块，不把全部教程堆进主文件 |
| JSON Schema | 通过 | Draft 2020-12模式自身检查及示例实例检查 |
| Python回归测试 | 26项通过 | 包括有效示例、错误ID、分支、越界、交叠、错误路线、缩放和输入/输出保护 |
| 示例规格 | 0错误、0警告 | 8节点、8边；名义几何无包围盒交叠/线路穿框等已实现检查问题 |
| 缩放/像素计算 | 通过 | 示例不缩放；180×130 mm、1000 dpi对应7087×5119像素（设计计算，并未导出图像） |
| 目录、相对文件链接、来源ID | 通过 | 本地包检查；不表示联网复测所有URL |
| 来源/能力登记 | 38项来源；62项能力条目 | 40项文档支持操作；22项索引级路由，非完整产品功能计数 |
| 发布文件完整性 | 使用manifest.sha256 | 校验清单覆盖除清单自身外的发布文件；打包后另作ZIP完整性检查 |

测试原始运行记录见[tests/last-run.txt](../tests/last-run.txt)。Python工具无第三方运行依赖；本次额外模式/YAML校验使用了环境预装的jsonschema与PyYAML，不是用户运行技能所必需。

## 没有执行，也不宣称通过

没有Windows Visio GUI实机操作、原生.vsdx生成/打开、PDF/SVG导出或渲染；没有检查真实字体嵌入、字形、节点文字溢出、实际连接点粘附或所有版本菜单。
没有对任一期刊执行完整终稿验收；没有将全部Visio教程逐篇穷尽；没有独立AI模型的技能触发率与实际任务成功率评测。
人工场景清单见[tests/scenarios.md](../tests/scenarios.md)，它不是已通过测试清单。

## 静态检查器的能力边界

几何依据轴对齐包围盒和用户给出的名义折线，不读取Visio真实几何。文本容纳是粗略估计，不读取实际字体度量。连接线之间的交叉、分支标签位置、容器内边距和所有字形不在脚本完整检查范围内。
图可达性只证明存在某条图路径，不证明条件可满足、算法收敛、循环一定终止或科研方法正确。期刊名称字段不会自动触发完整期刊规范验证。

## 重跑

```bash
python scripts/validate_package.py
python scripts/validate_spec.py examples/iterative-optimization/diagram-spec.json
python -m unittest discover -s tests -v
```

修改本包后哈希不再匹配属预期，需要按新版本重新生成清单。不要把哈希失配压制为“通过”。
