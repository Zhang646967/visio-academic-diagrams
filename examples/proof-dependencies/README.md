# 回归示例：证明依赖不等于执行顺序

[合成材料](synthetic-paper.md)中的depends_on从结论指向前提；[图计划](figure-plan.json)用reverse_relation=true显式反转，使图中箭头从前提指向结论。
[内容模型](paper-model.json)保留实数、有限序列条件；[布局](layout.json)保留输入可为负的限定。
此例只是类型和来源映射测试，不是对数学证明正确性的通用自动验证。
两条短连接的标签宜使用偏移标注，在真实Visio中检查文字位置；几何检查不会检测全部标签碰撞。
