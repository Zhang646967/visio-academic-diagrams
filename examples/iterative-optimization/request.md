# 示例输入（虚构教学任务，不是用户的真实研究）

用Windows桌面Visio画一个普通迭代优化流程：输入问题与参数，初始化可行候选并设t=0，评价候选并更新最好解。
每次评价后判断t是否达到非负整数T_max。达到就输出最好解并结束；否则产生下一可行候选，t加1，再回到评价。
假设初始化、候选生成和评价均在有限时间完成。用英文标签，最终180 mm宽，不指定期刊，要求手工操作说明。

这项假设不适用于所有约束优化问题：若实际方法可能无法找到可行点或候选生成失败，需要依据真实算法补充分支。

第一次评价时用当前候选初始化最好解；后续按问题已定义的优化方向比较更新。
